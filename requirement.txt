# This file list python modules that will be installed for
# global pip.
# Use virtualenv for each project to isolate python modules,
# only install necessary python modules.

ansible
Fabric
powerline
ptpython
Pygments
pylint
virtualenv
virtualenvwrapper
yapf
